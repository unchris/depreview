[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "depreview"
version = "0.0.1"
description = "Testing DepReview"
readme = "README.md"
authors = ["Chris Cameron"]

[tool.poetry.dependencies]
python = "~=3.10"
